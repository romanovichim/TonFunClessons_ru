import { BackoffFunc } from '../timer/backoff';
export declare class SyncValue<T> {
    private _updater;
    private _value;
    private _sync;
    constructor(initial: T, updater: (value: T) => Promise<void>, opts?: {
        backoff?: BackoffFunc;
    });
    get value(): T;
    set value(v: T);
}
