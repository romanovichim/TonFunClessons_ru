/// <reference types="node" />
declare const PACKET_MIN_SIZE: number;
declare class ADNLPacket {
    private _payload;
    private _nonce;
    constructor(payload: Buffer, nonce?: Buffer);
    get payload(): Buffer;
    get nonce(): Buffer;
    get hash(): Buffer;
    get size(): Buffer;
    get data(): Buffer;
    get length(): number;
    static parse(data: Buffer): ADNLPacket | null;
}
export { ADNLPacket, PACKET_MIN_SIZE };
