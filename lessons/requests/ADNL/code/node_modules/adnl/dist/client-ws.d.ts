/// <reference types="node" />
import { ADNLClient } from './client';
declare class ADNLClientWS extends ADNLClient {
    private url;
    constructor(url: string, peerPublicKey: Uint8Array | string);
    private parse;
    protected onHandshake(): void;
    connect(): Promise<void>;
    end(): void;
    write(data: Buffer | Uint8Array): void;
}
export { ADNLClientWS };
