declare class ADNLKeys {
    private _peer;
    private _public;
    private _shared;
    constructor(peerPublicKey: Uint8Array);
    get public(): Uint8Array;
    get shared(): Uint8Array;
    generate(): Promise<void>;
}
export { ADNLKeys };
