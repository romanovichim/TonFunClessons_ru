import { BackoffFunc } from "../timer/backoff";
export declare class InvalidateSync {
    private _invalidated;
    private _invalidatedDouble;
    private _stopped;
    private _command;
    private _backoff;
    constructor(command: () => Promise<void>, opts?: {
        backoff?: BackoffFunc;
    });
    invalidate(): void;
    stop(): void;
    private _doSync;
}
